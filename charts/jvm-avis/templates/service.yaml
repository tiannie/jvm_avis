apiVersion: v1
kind: Service
metadata:
  name: {{ include "jvm-avis.fullname" . }}
  labels:
    {{- include "jvm-avis.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jvm-avis.selectorLabels" . | nindent 4 }}
